spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hibernate
    username: root
    password: *
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
